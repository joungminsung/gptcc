{
  "description": "gptcc: start the local proxy on session start (cross-platform)",
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/start-proxy.mjs\"",
        "timeout": 10,
        "async": true
      }
    ]
  }
}
